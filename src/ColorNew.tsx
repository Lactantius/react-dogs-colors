import React from "react";

import { ColorProps } from "./types";

function ColorNew({ colors, add }: ColorProps): JSX.Element {
  return <div className="ColorNew"></div>;
}

export default ColorNew;
